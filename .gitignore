# Legacy (solo referencia, no se versiona en el repo nuevo)
/_legacy/

# Dependencias
node_modules/

# Python
__pycache__/
*.pyc

# Backend (Maven / Spring Boot)
/backend/target/

# Frontend (Angular)
/frontend/dist/
/frontend/.angular/

# Entornos: ignora todos los .env excepto los ejemplos versionados
.env
.env.*
!.env.example
!**/.env.example

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Datos crudos externos sujetos a acuerdo de uso (Fase 5, NPDB/HRSA):
# el CSV NUNCA entra al repo. Solo el script y su hash SHA-256 se versionan.
# Ver docs/calibracion-generador.md.
NPDB*.CSV
NPDB*.csv
NPDB*.DAT
NPDB*.POR
ml-service/evaluation/calibration/data/
