# ─── Frontend (Angular) ────────────────────────────────────────────────
# La URL base del backend se inyecta via environment.ts (dev) y la config de
# Vercel (prod). Este archivo documenta el valor esperado.

# Backend Spring Boot — dev local / URL de Railway en prod
API_BASE_URL=http://localhost:8080
